/**
 * VueRouter
 * @class VueRouter
 * @classdesc VueRouter
 */
class VueRouter {
  /**
   * constructor
   * @param config
   */
  constructor(config) {
    this.$config = config;
  }
}

export default VueRouter;
